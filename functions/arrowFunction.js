const funcaoArrow = () => {
    console.log("Sou uma arrowFunction")
}
funcaoArrow()

// ArrowFunction se chama assim pois parece uma flecha, e tem sintaxe diminuida

const soma = (a, b, c) => {
    return a + b + c
}

console.log(soma(10, 20, 30))

// A ArrowFunction funciona como uma função normal, aqui podemos ver parametros sendo passados